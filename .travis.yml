dist: trusty
sudo: false

language: node_js
node_js: node
cache: yarn

before_script:
  - yarn lint

script:
  - yarn build-production

deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  local_dir: public
  on:
    branch: master
