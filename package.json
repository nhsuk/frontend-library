{
  "name": "frontend-library",
  "version": "0.12.0",
  "private": true,
  "scripts": {
    "build": "yarn brunch -- build",
    "build-production": "yarn brunch -- build --production",
    "lint": "eslint --ext .js,.json .",
    "prepush": "yarn lint",
    "start-clean": "rm -rf ./public && yarn start-watch",
    "start-watch": "yarn brunch -- watch --server"
  },
  "dependencies": {
    "eslint-config-nhsuk": "^0.14.0"
  },
  "devDependencies": {
    "brunch": "^2.10.10",
    "eslint": "^4.6.1",
    "eslint-config-airbnb-base": "^12.0.0",
    "eslint-plugin-import": "^2.7.0",
    "eslint-plugin-json": "^1.2.0",
    "eslint-plugin-mocha": "^5.0.0",
    "husky": "^1.3.0",
    "sass-brunch": "^2.10.4"
  }
}
